{% extends 'base.html.twig' %}

{% block title %}MessageRequest{% endblock %}

{% block body %}
    <h1>MessageRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message_request.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message_request.message }}</td>
            </tr>
            <tr>
                <th>DateCreated</th>
                <td>{{ message_request.dateCreated ? message_request.dateCreated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('message_request_index') }}">back to list</a>

    <a href="{{ path('message_request_edit', {'id': message_request.id}) }}">edit</a>

    {{ include('message_request/_delete_form.html.twig') }}
{% endblock %}
